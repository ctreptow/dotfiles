name: pdf_helper
root: ~/code/work/pdf_helper
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rackup config.ru -p 3999
