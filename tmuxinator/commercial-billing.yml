name: commercial-billing
root: ~/code/work/commercial-billing
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rails server -p 3002
