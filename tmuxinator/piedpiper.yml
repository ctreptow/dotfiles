name: piedpiper
root: ~/code/work/piedpiper
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rails server -p 3003
