name: undertaker
root: ~/code/work/undertaker
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rails server -p 3004
