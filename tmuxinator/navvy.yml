name: navvy
root: ~/code/work/navvy
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec shotgun config.ru -p 3006
