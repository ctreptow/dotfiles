name: rate
root: ~/code/work/rate_against_the_machine
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rackup config.ru -p 9292
