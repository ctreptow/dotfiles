name: streamcatcher
root: ~/code/work/streamcatcher
windows:
  - shell:
      layout: main-horizontal
      panes:
        - shell:
        - shell:
        - shell:
  - server: bundle exec rackup config.ru -p 9294
